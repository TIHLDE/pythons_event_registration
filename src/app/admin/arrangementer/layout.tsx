import { Metadata } from 'next';

import AdminLayout from '~/components/AdminLayout';

export const metadata: Metadata = {
  title: 'Admin Arrangementer - TIHLDE Pythons',
};

export default AdminLayout('Arrangementer');
