import { Metadata } from 'next';

import AdminLayout from '~/components/AdminLayout';

export const metadata: Metadata = {
  title: 'Admin Lag - TIHLDE Pythons',
};

export default AdminLayout('Lag');
